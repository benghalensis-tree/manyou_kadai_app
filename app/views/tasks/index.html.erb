<h1>タスク一覧</h1>

<% @tasks.each do |task|%>
<p>
  <%= "タスク名"%>
  <%= task.task_name %>
  <%= "内容"%>
  <%= task.content %>
  <%= "ステータス"%>
  <%= task.status %>
  <%= "優先順位"%>
  <%= task.priority %>
  <%= "期限"%> 
  <%= task.end_date %>
</p>

<% end %>